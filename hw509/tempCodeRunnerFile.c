      // Node* newNode = malloc(sizeof(Node));
      // newNode->val = val;

      // direction = randomInt(1, 2, root->val, val);

      // if(direction == 1){
      //    root->left = newNode;
      // }else if(direction == 2){
      //    root->right = newNode;
      // }